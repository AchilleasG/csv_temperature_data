services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=${APP_ENV:-development}
      - CSV_PATH=${CSV_PATH:-/app/data/temperature_data.csv}
    volumes:
      - ./data:/app/data:ro
